For cata 71 I created a matching game with pairs of cards. To do this, I used an HTML document to generate the view structure, a cascade CSS stylesheet to apply different styles, 
and I used tools like Google Fonts for font styles and ChatGPT to generate the board and card images.
